"""
旧 Web API 路由模块。

阶段 1 冻结说明：
1. 该模块服务于旧网页聊天 / 浏览器录音上传方向。
2. 当前仓库进入桌宠重建阶段后，不再继续扩展这里的主链路接口。
3. 旧路由设计与新协议方向存在差异，例如裸路径字段、Web 上传语义等。

后续约束：
- 新方向优先在 core / services / desktop 新骨架内落地。
- 如需参考旧接口，仅用于迁移分析，不直接复用为主实现。
"""
