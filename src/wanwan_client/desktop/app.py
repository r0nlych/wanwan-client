"""
旧 Web 壳入口。

阶段 1 冻结说明：
1. 该文件属于旧 Flask + 网页聊天页方向，只保留为历史参考。
2. 当前项目目标已切换为 Windows 桌宠式语音助手 MVP。
3. 新方向默认由 desktop 本地 UI / 输入输出交互承担，不再继续扩展本文件。

后续约束：
- 不在这里新增桌宠主逻辑。
- 不在这里继续扩展旧网页聊天能力。
- 如确需读取旧实现细节，仅作为迁移参考，不作为新骨架入口。
"""
