# 2026-04-20 项目日报

## 今日目标
- 完成架构收口方案，解决 app.py 职责过多、infrastructure 层未真正承担职责、services 层和接口响应格式耦合过深三个核心问题
- 验证重构后主链路（录音上传 → STT 识别 → 回填）完整可用
- 确保接口严格遵循 `docs/internal-api-spec.md`

## 实际完成内容
**服务集成工程师任务：**
- 修改 `src/wanwan_client/services/stt/local_stt_client.py`：`transcribe()` 方法返回纯业务结果结构 `{"success": bool, "text": str, "language": str, "error_message": str}`，解耦 HTTP 响应格式

**桌面端路由工程师任务：**
- 新建 `src/wanwan_client/infrastructure/http/response_builder.py`：统一响应组装类，严格遵循接口规范
- 完善 `src/wanwan_client/infrastructure/file_system/file_store.py`：统一文件操作（目录创建、录音保存、文件查找）
- 新建 `src/wanwan_client/desktop/routes/api_router.py`：承载 `/api/record`、`/api/stt` 路由，负责参数校验、调用 service、组装响应
- 修改 `src/wanwan_client/desktop/app.py`：收口至 58 行，只保留应用创建、路由注册、启动入口，移除原有业务逻辑

**测试排障工程师任务：**
- 验证 STT 功能：`test_stt.py` 通过
- 修复路径问题：app.py 中 `send_file()` 使用绝对路径确保跨环境兼容
- 验证完整主链路：创建 `test_integration.py` 验证录音上传 → STT 识别 → 回填流程

**前端工程师验证：**
- 优化 `src/wanwan_client/desktop/static/js/chat_page.js`：STT 请求格式从 `audio_path` 改为 `payload.input_audio_path`，完全遵循接口规范

## 完成度
- 架构收口方案：100% 完成
- 主链路验证：100% 通过
- 接口规范符合性：100% 对齐

## 关键问题 / 难点
1. **终端编码显示乱码**：STT 返回的中文在 Windows 终端显示为乱码
2. **路径解析错误**：app.py 中 `send_file()` 相对路径解析与项目结构不匹配
3. **STT 请求格式不规范**：前端使用顶级 `audio_path` 而非 `payload.input_audio_path`

## 解决状态
1. **终端编码显示乱码**：已确认非功能问题
   - 定位：UTF-8 字节正确，是 Windows 终端 GBK 编码显示问题
   - 结论：功能正常，不阻塞推进

2. **路径解析错误**：已解决
   - 解决方法：改用 `os.path.join(os.path.dirname(__file__), ...)` 绝对路径

3. **STT 请求格式不规范**：已解决  
   - 解决方法：前端更新为 `payload.input_audio_path` 格式，后端路由层保持双重解析兼容

## 当前项目状态
- **record_upload**：已完成（接口规范符合，文件保存由 FileStore 统一管理）
- **stt**：已完成（本地 Whisper tiny 模型，服务层纯化，接口规范符合）
- **llm**：未开始
- **tts**：未开始  
- **rvc**：未开始
- **playback**：部分完成（`/api/audio` 路由保留在 app.py，未迁移）

## 明日计划
**服务集成工程师任务（LLM 接入）：**
- 文件：`src/wanwan_client/services/llm/deepseek_client.py`
- 目标：实现 DeepSeek 客户端，提供 `generate(prompt, history)` 方法
- 产出：LLM 服务封装，返回纯业务结果结构
- 验收标准：支持中文对话，返回格式符合 `{"success": bool, "reply_text": str, "error_message": str}`

**桌面端路由工程师任务（LLM 路由）：**
- 文件：`src/wanwan_client/desktop/routes/api_router.py`
- 目标：新增 `/api/llm` 路由，处理 LLM 请求
- 产出：完整路由实现，遵循接口规范
- 验收标准：接收 `payload.user_text`、`payload.history`，返回 `payload.reply_text`

**前端工程师任务（发送按钮集成）：**
- 文件：`src/wanwan_client/desktop/static/js/chat_page.js`
- 目标：为发送按钮添加点击事件，调用 LLM 接口
- 产出：前端完整交互链（录音 → STT → LLM → 显示回复）
- 验收标准：用户可发送文本消息，收到 LLM 回复显示在聊天记录区

## 备注 / 关键结论
1. **终端编码问题已确认非功能缺陷**：UTF-8 字节传输正确，乱码仅出现在 Windows 终端显示层，不影响实际功能
2. **接口规范已完全对齐**：所有新增/修改接口均遵循 `docs/internal-api-spec.md` 外层字段结构
3. **架构收口验证完成**：app.py 成功收口（58 行），infrastructure 层承担职责，service 层纯化，主链路功能完整
4. **STT 请求格式统一**：前端已更新为规范格式，后端保持双重解析确保平滑过渡
5. **路径兼容性解决**：使用绝对路径确保跨环境稳定运行

---

**如果今日提前完成**：开始 TTS 接入准备，调研 AllTalk 客户端实现方案。